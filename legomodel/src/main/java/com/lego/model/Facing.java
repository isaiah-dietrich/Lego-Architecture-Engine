package com.lego.model;

/**
 * Cardinal direction a directional part faces (slopes, curves, wedges).
 * NONE indicates a standard non-directional part.
 */
public enum Facing {
    NONE,
    NORTH,
    EAST,
    SOUTH,
    WEST
}
